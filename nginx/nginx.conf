upstream web {
   ip_hash;
   server web:8000;
 }

 # portal
 server {
   location / {
         proxy_pass http://web;
     }
   listen 80;

   location /static {
     autoindex on;
     alias /src/static/;
   }
 }